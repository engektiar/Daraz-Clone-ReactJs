import { Container, Grid } from "@mui/material";
import React from "react";

const LodeMorefile = () => {
  return (
    <Container>
      <Grid container spacing={2}>
        <Grid item xs={12}></Grid>
      </Grid>
    </Container>
  );
};

export default LodeMorefile;
